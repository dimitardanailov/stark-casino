import { InternalMetadata } from "@/seo/metadata";

const title = "Casino";
const description = "casino";
const canonical = "";

const props: InternalMetadata = {
  title,
  description,
  canonical,
};

export default props;

export { title };
